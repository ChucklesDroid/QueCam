// Data Structure representing batch of video frames

typedef struct Videoframes{
	char location[40] ; 										// location of frames batch
	int deviceId ; 						 							// Terminating no of its address
} videoframes;
